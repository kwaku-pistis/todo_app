<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="arch.list">
      <field name="name">arch list</field>
      <field name="model">arch.arch</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="arch.action_window">
      <field name="name">arch window</field>
      <field name="res_model">arch.arch</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="arch.action_server">
      <field name="name">arch server</field>
      <field name="model_id" ref="model_arch_arch"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="arch" id="arch.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="arch.menu_1" parent="arch.menu_root"/>
    <menuitem name="Menu 2" id="arch.menu_2" parent="arch.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="arch.menu_1_list" parent="arch.menu_1"
              action="arch.action_window"/>
    <menuitem name="Server to list" id="arch" parent="arch.menu_2"
              action="arch.action_server"/>
    -->
  </data>
</odoo>